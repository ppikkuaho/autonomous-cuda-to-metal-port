# Compatibility Matrix

This matrix tracks actionable compatibility decisions, the result of a static
scan of the upstream tree for CUDA and backend assumptions.

| CUDA/Linux component or assumption | Pixal3D location | Purpose | Mac strategy | Verification | Status | Notes |
|---|---|---|---|---|---|---|
| `flash_attn` / `xformers` | attention modules / env defaults | transformer attention | PyTorch SDPA for the selected Mac inference path | `test_030_sdpa.py`, end-to-end geometry runs | pass | Runs used `ATTN_BACKEND=sdpa`, `SPARSE_ATTN_BACKEND=sdpa`, and strict MPS fallback disabled |
| `flex_gemm` sparse conv | `pixal3d/modules/sparse/conv/*` | sparse 3D submanifold convolution | `SPARSE_CONV_BACKEND=none` via pure PyTorch `conv_none.py`; revisit `mtlgemm` later for acceleration | `test_050_sparse_conv.py`, end-to-end geometry runs | pass | Correctness fallback covers dispatch, CPU/MPS reference matching, sparse reductions, duplicate-coordinate rejection, stale-cache safety, empty tensors, streaming/chunked lookup, and a `SparseConvNeXtBlock3d` smoke. Slow and submanifold-only; dense 12-step high-res latent replay still hits scale limits. |
| `flex_gemm` grid sampling | `pixal3d/representations/mesh/base.py`, texture export | 3D attribute sampling for mesh/texturing | guarded import; the texture exporter implements sparse 8-neighbor trilinear sampling mirroring `flex_gemm.grid_sample_3d` neighbor/weight semantics | mesh-extraction and texture-export checks, texture boundary capture | pass | `query_attrs` still raises if called without a backend; the selected exporter has its own sparse trilinear sampler. The MPS `grid_sample` fallback also fixed a texture-color sampling bug that had inflated base-color error |
| `o_voxel.convert.flexible_dual_grid_to_mesh` | `pixal3d/models/sc_vaes/fdg_vae.py` | dual-grid mesh extraction from decoded shape latents | local pure Python/PyTorch fallback in `pixal3d/utils/mesh_extract.py` | `test_060_mesh_extract.py`, CUDA mesh-replay check, `validate_glb.py` | pass | Synthetic quad/empty cases pass on CPU/MPS; `fdg_vae` imports without `o_voxel._C`; the CUDA 12-step FDG tensor mesh replay matched CUDA native vertex/face counts exactly. |
| `o_voxel.postprocess.to_glb` | `inference.py`, texturing paths | final textured GLB postprocess/remesh/bake | sparse-trilinear / xatlas PBR bake with official-style UV-valid mask inpainting, plus a native Metal narrow-band remesh path for shell solidity | `tests/test_090_geometry_smoke.py`, the texture-export run, the final-candidate parity gate set, `validate_glb.py` | pass | Produces a valid textured GLB with one material/texture. One integrated native candidate clears all 13 final-candidate parity gates against the CUDA reference under a 4M-sample gate stable across seeds 42/43/44, plus human visual acceptance — for that exact candidate/reference pair only. Not a general texture-quality claim |
| `cumesh` / narrow-band remesh | mesh cleanup and texturing | hole filling/simplification, shell closure/remesh | native Metal narrow-band remesh path; cheaper shell-thickening fallbacks were tried and rejected against the CUDA pre-export to final shell target | GLB validation, shell-area/solidity diagnostics, final-candidate parity gates | partial | Native Metal remesh supplies the side/back solidity that CUDA gets from `o_voxel.postprocess.to_glb(remesh=True)`; a silent node-dropping traversal bug in the Metal BVH kernel was fixed (shell-area parity ~0.33 -> ~0.99 via stackless traversal). Requires the native Metal toolchain row below |
| `nvdiffrast` / `nvdiffrec_render` | renderers/texturing | rasterization and texture baking | CPU xatlas/UV unwrap + raster mask in the texture exporter; GPU renderers still deferred | texture-export run, final-candidate parity gates | partial | The xatlas/UV atlas path is validated for the frozen target; CUDA GPU rasterizer/baker parity remains future work |
| native Metal toolchain (`metal` / `metallib`, `mtlbvh`, Metal `cumesh`) | texture/remesh build | compiling the native Metal narrow-band remesh and BVH kernels | build the native Metal remesh branch with the full Metal compiler toolchain | toolchain build probe, shell-area parity diagnostics | partial | Documented dependency: building the native Metal remesh branch requires the full Metal compiler tools (`xcrun metal` / `metallib`), not Command Line Tools alone. Without it the remesh branch falls back and full shell solidity is not reproduced |
| `.cuda()` / `device="cuda"` | `inference.py`, `app.py`, image conditioning, pipeline helpers | device movement | device helper and Mac CLI path; runtime guard | `test_020_device_hygiene.py`, static scan diff | partial | Mac CLI path patched; app/training/toolkit deferred |
| `torch.cuda.*` cache/runtime | inference/app/training/data code | cache, synchronization, diagnostics | guarded no-op or device-specific sync/cache helper | `test_020_device_hygiene.py` | partial | Inference-critical pipeline calls patched; training/toolkit deferred |
| `PYTORCH_CUDA_ALLOC_CONF` | `inference.py`, `app.py` | CUDA allocator tuning | omit on Mac path | import tests | partial | `inference.py` no longer sets it; `app.py` deferred |
| Pixal3D projection `grid_sample(..., border)` | `image_conditioned_proj.py` | view-aligned image feature sampling | integrated tested clamped-grid MPS shim from `pixal3d/utils/projection.py` | `test_040_projection_math.py`, `tests/test_070_image_conditioning.py`, end-to-end runs | pass | Real DINO projection conditioning ran in the end-to-end geometry runs |
| DINOv3 projection | image-conditioning mixin | Pixal3D-specific features | use accessible `camenduru/dinov3-vitl16-pretrain-lvd1689m`; support current Transformers `model.model.layer` layout | end-to-end runs | pass | Official `facebook/...` repo remains gated, but equivalent mirrored weights loaded and ran |
| NAF | image-conditioning mixin/configs | high-res feature upsampling | native `natten-mps` where available; explicit `PIXAL3D_NAF_BACKEND=interpolate` fallback preserving `embed_dim * 2` contract | `tests/test_070_image_conditioning.py`, end-to-end runs | partial | Native NAF/natten-mps imports and runs for the tested image-conditioning path; interpolation remains a fallback. Neither path is the basis of a general texture/style parity claim. |
| RMBG/BiRefNet | pipeline rembg config | background removal/preprocess | lazy load; bypass with RGBA alpha input | end-to-end runs | partial | Gated `briaai/RMBG-2.0` no longer blocks pipeline load; non-alpha inputs still need access or an alternate rembg |
| MoGe | `inference.py`, `app.py` | automatic camera estimation | install MoGe dependency and run on MPS after geometry works | auto-camera geometry run | pass | Auto camera produced finite params and a valid GLB; install updated `utils3d` to 1.3 in the venv |
| training/data toolkit CUDA | `train.py`, `pixal3d/trainers/*`, `data_toolkit/*` | training and preprocessing | defer; do not patch unless imported by inference | none initially | deferred | Out of scope for the inference path |
